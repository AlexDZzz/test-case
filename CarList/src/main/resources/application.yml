spring:
  datasource:
    url: jdbc:mysql://localhost:3306/embedika?useSSL=false
    username: root
    password: Shtora-4
  jpa:
    hibernate:
      ddl-auto: create
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %-5level - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
  file: /logs/app.log
