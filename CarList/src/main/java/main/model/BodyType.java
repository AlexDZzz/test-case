package main.model;

/**
 * Enum, as storage for possible values.
 *
 * @see Car
 */
public enum BodyType
{
    SEDAN,
    COUPE,
    WAGON,
    HATCHBACK,
    CONVERTIBLE,
    SUV,
    MINIVAN,
    NOT_SPECIFIED
}
